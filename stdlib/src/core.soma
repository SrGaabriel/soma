use base::prim::{IO, Int32, Pair}
use base::string::{String}

@[extern "puts"]
def c_puts : String -> Int32

def puts (s : String) : IO ()
    | w =>
        let _ := c_puts s in
        Pair::Mk w ()