pub use base::prim::{+, -, *, /, %, String, IO, List}
pub use base::core::{Option, Some, None, Either, Err, Ok, Display, display, Semigroup, <>, Monoid, mempty, mconcat, Functor, fmap, Applicative, pure, <*>, *>, <*, Monad, >>=, >>}
pub use stdlib::core::{puts}
