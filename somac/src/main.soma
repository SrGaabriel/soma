use stdlib::program::{get_args}
use stdlib::collections::{any}

def main : IO () := compose
    bind args <- get_args
    let some_has := any (\arg -> arg == "hello") args
    when some_has (puts "You said hello!")
