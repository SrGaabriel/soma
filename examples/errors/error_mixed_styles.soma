// Error: Mixed parameter styles

intrinsic def {+} :: Int -> Int -> Int

// Mixing typed and untyped parameters - should fail
def mixedParams(x: Int, y) -> Int = x + y

// This should parse
def properParams(x: Int, y: Int) -> Int = x + y
