// Error: Missing parameter in function definition

intrinsic def {+} :: Int -> Int -> Int

// Missing closing paren
def brokenFunc( -> Int
    | x => x + 1

// This should still parse if recovery works
def goodFunc(x: Int) -> Int = x + 1
