// Error: Missing arrow in type signature

intrinsic def {+} :: Int -> Int -> Int

// Missing arrow between parameter and return type
def badType(x: Int, y: Int) Int = x + y

// This should parse if recovery works
def goodType(x: Int, y: Int) -> Int = x + y
