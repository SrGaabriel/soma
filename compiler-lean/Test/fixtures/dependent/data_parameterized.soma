// expect: success
// Test: parameterized algebraic data type

data Maybe a
    | Nothing
    | Just a

def fromMaybe :: forall a. a -> Maybe a -> a
    | default Nothing => default
    | _ (Just x) => x
