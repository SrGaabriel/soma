// expect: success
// Test: unrestricted (omega) quantity annotation - use any number of times

def useMany :: (ω a : Type) -> a -> a
    | _ x => x

def duplicate :: (ω x : Int) -> (Int, Int)
    | x => (x, x)
