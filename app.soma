fibonacci :: (Int) -> Int
  | 0 -> 0
  | 1 -> 1
  // | n -> fibonacci(n-1) + fibonacci(n-2)

nothing :: Int = (5+5)*5

hello :: String =
    "Hello, " + "World!"

test :: Int =
    5 + sum 5 6

sum a b :: (Int Int) -> Int =
    5 + a + nothing