use test/prim.{Int}

@[intrinsic "primop.add"]
def {+} : Int -> Int -> Int

@[intrinsic "primop.mul"]
def {*} : Int -> Int -> Int

def double (x : Int) : Int = x + x

def square (x : Int) : Int = x * x

def main : Int = (double 17 + square 4)
