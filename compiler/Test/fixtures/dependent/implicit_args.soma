// expect: success
// Test: implicit argument syntax

def id : ∀ a. a -> a
    | x => x

def apply : ∀ a b. (a -> b) -> a -> b
    | f, x => f x

def test_id : Int = id 42
def test_apply : Int = apply id 100
