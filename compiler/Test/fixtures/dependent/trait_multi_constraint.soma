// expect: success
// Test: multiple class constraints

class Eq a where
    eq : a -> a -> Bool

class Ord a where
    compare : a -> a -> Int

def sorted_eq (a : Type) : {{Eq a}} -> {{Ord a}} -> a -> a -> Bool
    | x, y => eq x y
