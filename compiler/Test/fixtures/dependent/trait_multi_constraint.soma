// expect: success
// Test: multiple class constraints

class Eq a where
    def eq :: a -> a -> Bool

class Ord a where
    def compare :: a -> a -> Int

def sortedEq :: forall a. a -> a -> Bool with (Eq a, Ord a)
    | x y => eq x y
