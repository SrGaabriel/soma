// expect: success
// Test: recursive algebraic data type

data List a
    | Nil
    | Cons a (List a)

def head :: forall a. a -> List a -> a
    | default List::Nil => default
    | _ (List::Cons x _) => x
