// expect: success
// Test: simple pair types with @[wired_in] attributes

data Pair a b
    | MkPair a b

def pair :: (Int, Int) = (1, 2)

def fst :: (Int, Int) -> Int
    | (x, _) => x

def snd :: (Int, Int) -> Int
    | (_, y) => y
