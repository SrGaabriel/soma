// expect: success
// Test: pattern matching on ADT - MAT node lowering

data Color
    | Red
    | Green
    | Blue

def toInt :: Color -> Int
    | Red => 0
    | Green => 1
    | Blue => 2

def test1 :: Int = toInt Red
def test2 :: Int = toInt Green
def test3 :: Int = toInt Blue
