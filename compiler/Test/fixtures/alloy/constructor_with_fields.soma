// expect: success
// Test: constructors with fields - CTOR with arity > 0

inductive Maybe (a : Type) where
    | Nothing
    | Just (value : a)

def from_maybe :  a. a -> Maybe a -> a
    | default, Nothing  => default
    | _ (Just, x)  => x

def test : Int32 := from_maybe 0 (Just 42)
