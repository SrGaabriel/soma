// expect: success
// Test: constructors with fields - CTOR with arity > 0

data Maybe a
    | Nothing
    | Just a

def fromMaybe :: forall a. a -> Maybe a -> a
    | default Nothing => default
    | _ (Just x) => x

def test :: Int = fromMaybe 0 (Just 42)
