// expect: success
// Test: closure with captured variable - environment lowering

@[intrinsic "primop.add"]
def {+} : Int -> Int -> Int

def make_adder : Int -> Int -> Int
    | n => \x -> x + n

def test : Int = (make_adder 10) 5
