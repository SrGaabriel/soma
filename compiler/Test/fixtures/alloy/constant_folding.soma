// expect: success
// Test: constant expressions - NUM node lowering

@[intrinsic]
def {+} :: Int -> Int -> Int

@[intrinsic]
def {*} :: Int -> Int -> Int

def fortyTwo :: Int = 42
def sum :: Int = 1 + 2 + 3 + 4 + 5
def product :: Int = 2 * 3 * 4

def test :: Int = fortyTwo + sum + product
