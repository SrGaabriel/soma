// expect: error type mismatch

record Point where
    x : Int
    y : Int

record Vec2 where
    x : Int
    y : Int

def needs_point : Point -> Int
    | p => p.x

def test : Int = needs_point (Vec2 1 2)
