// expect: error type mismatch

record Point where
    x : Int32
    y : Int32

record Vec2 where
    x : Int32
    y : Int32

def needs_point : Point -> Int32
    | p => p.x

def test : Int32 = needs_point (Vec2 1 2)
