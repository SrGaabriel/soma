// expect: success

class Functor (f : Type -> Type) where
    def fmap :  a b. (a -> b) -> f a -> f b

def map_both :  (f : Type -> Type) a b. (a -> b) -> f a -> f a -> (f b, f b) with (Functor f)
    | fn x, y  => (fmap fn x, fmap fn y)
