// Fixture: implicits ghost view

@[wired_in "type.int"]
inductive Int32 where
    | MkInt32

@[wired_in "type.string"]
record String where
    data : Type
    len  : Type

def id {a : Type} (x : a) : a := x

def x : Int32 := id "hello"
